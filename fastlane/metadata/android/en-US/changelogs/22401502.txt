REALITY nodes with the firefox and safari fingerprints work again: the core carries the Firefox 148 and Safari 26.3 profiles with the key share Xray servers since v26.9.8 demand. AmneziaWG links holding a whole configuration file no longer lose the node. Workspaces: loading a slot rebuilds the config. Rule editor: Save in the top bar validates the JSON. Restoring a backup with Replace keeps the startup questions answered. Tailscale: the state directory belongs to the node.