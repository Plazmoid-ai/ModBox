The node editor no longer loses TLS fields such as a NaiveProxy root cert (#140): the model now carries every TLS key the core knows. New Source and JSON tabs: Source is the node's original text and the only place you edit, JSON shows what the core receives; a JSON source goes to the core as is, checked by the core on save. WireGuard .conf is stored as the file, tag aside. Core v1.14.1-lx.4: TLS fragmentation applies to REALITY, per-node key_share hybrid or classical. TCP keep-alive per node.
