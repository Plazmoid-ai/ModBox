Simplified Chinese joins English and Russian: the whole interface, the node wizard, routing and DNS, plus the notification, Quick Settings tile and shortcuts. Pick it in Settings, General, Language, or leave the setting on System and it follows the device. The translation was contributed by wyphgsy. The localization checks now cover every language instead of Russian alone, so a new language cannot fall behind as strings are added.
