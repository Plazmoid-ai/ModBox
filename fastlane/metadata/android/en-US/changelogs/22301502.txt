The tunnel comes back on its own after the process is killed: START_STICKY plus an AlarmManager watchdog (sticky alone loses a race inside system_server on a VpnService). A VPN in a work profile is no longer mistaken for a conflicting one, and Start no longer crashes on Android 10. New Region setting next to the language. All three install sources are always visible in About. The "(recommended)" mark no longer leaks into WARP configs. RECORD_AUDIO removed from the APK.
