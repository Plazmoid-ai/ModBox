# 438 — Контракт ## 16: чтение и запись LX Backup 1.0

| Поле | Значение |
|------|----------|
| Статус | **Released в v2.24.0** (15.09.2026, ядро `v1.14.0-lx.39`). Done; писатель и часть читателя заменены [§439](../features/439%20storage-contract-1-0/spec.md) до выпуска |
| Дата старта | 2026-09-14 |
| Дата завершения | 2026-09-14 |
| Коммиты | `86803db6` feat(438): чтение 1.0 одним слиянием с 0.x; `9e5aa5e9` docs(438); `feat(438): запись LX Backup 1.0` — писатель 1.0 вместо 0.12, ответы лаунчера, синк контракта 1.0.0 |
| Триггер | `app/contract/TASKS_LXBOX.md` ## 16 (контракт 1.0, D-109); решение владельца 14.09.2026: в этом же релизе LxBox пишет 1.0, экспорт 0.12 убран (D-110 — обе стороны пишут только 1.0) |
| Связанные | [§435](../features/435%20node-sections-tailscale/spec.md) (секции узла, кодек записей; §9.7 — пробелы кодека), [§434](434-srs-rule-multiple-rule-sets.md) (`refs[]`), [§407](407-backup-corpus-pre-state-merge.md) (слияние по идентичности, предсостояние корпуса), [§370](370-rule-order-num-axis.md) (ось `num`), контракт `docs/BACKUP.md` §2, §6, §9, §11, `docs/NODE_SECTIONS.md` §1, §5, `docs/ONE_NAMESPACE.md` §1 |

> **Заменено §439 (develop `230c3354`, до выпуска 2.23.3).** Переводчик между
> формой хранения 2.23.2 и файлом 1.0, описанный ниже (`_subscription10ToJson`,
> `_server10ToJson`, `_folder10ToJson`, `_chain10ToJson`, `_rule10ToJson`,
> `_dns10ToJson`, `_presetServerRecord`, `_LinkIndex`), удалён: хранение само
> держит записи 1.0, экспорт — срез хранения тем же кодеком
> (`lib/services/lx_backup_slice.dart`), импорт 1.0 разбирает записи кодеком
> хранения. Ссылки `hops[]`/`detour` в хранении и файле — `{folder_id, tag}` с
> сырым тегом члена (D-112), а не финальные теги; член папки `kind: auto`
> ввозится autogroup'ом, а не `backup_source_kind_unsupported`; одинаковые
> DNS-правила файла ввозятся все. Строки настроек в таблице «Что LxBox в 1.0
> не выражает» (import-rules, политика detour, префикс сервера, ping-опции
> папки, `label` цепочки, `varValues` и `description` DNS-сервера) устарели с
> контракта 1.0.1: поля объявлены в `BACKUP.md` §2 «Поля стороны LxBox»,
> экспорт их пишет, импорт применяет (§439 §1.3, §6.4). Названными потерями из
> этой таблицы остались srs-правила DNS и json-правило с нечитаемым текстом.
> Разделы «Запись», «Ссылки», «Что не применяется на импорте» ниже описывают
> состояние на 14.09 и оставлены как история. Действующее описание — [§439](../features/439%20storage-contract-1-0/spec.md)
> и `docs/STORAGE.md`.

## Что и почему

Контракт 1.0 снял маппер между файлом и состоянием лаунчера: запись файла —
запись состояния («метаданные + `body`»). Четыре секции 0.12
(`subscriptions[]`/`servers[]`/`chains[]` + `folder`) свернулись в одну
`sources[]` с `kind`, у папки появилась своя запись с `id`, правило и
DNS-запись несут тело sing-box в `body`, у узла есть `sections`, ссылки на узел
(`hops[]`, `detour`) — объекты `{folder_id?, tag}`.

Шаг 1 — чтение 1.0 вместе с legacy 0.x: каждый вход превращает файл в одни и те
же промежуточные записи, дальше один код слияния (норма §9: второй набор правил
под второй формат дал бы на одном файле два разных состояния). Шаг 2 — запись
1.0 вместо 0.12: лаунчер 1.6.0 читает оба формата и сам пишет только 1.0.

## Что сделано

### Чтение

| Слой | Было | Стало |
|---|---|---|
| Разбор | `parseLxBackup` — один декодер 0.x; файл `lx_backup: 2` — отказ «новее поддерживаемого» | диспетчер по `lx_backup`: `_parse0x` (прежний, общие куски вынесены: Направления, `vars`, `route.final`, `warp[]`) и `_parse10`; вход `>2` и `<1` — отказ |
| Промежуточные записи | `LxSubscription`, `LxServer`, `LxDns` формы 0.12 | + `LxSubscription.id`/`fullSettings`/`position`, `LxServer.folderRef`/`id`/`sections`/`sectionsPresent`/`detour`/`position`, новые `LxFolder` и `LxNodeLink` (`{folder_id, tag}`), `LxDns.defaultDomainResolver`, `LxBackupFile.folders`/`chainHops` |
| `sources[]` 1.0 | — | `subscription` → имя, `enabled`, префикс `tag_policy`, `update.interval_hours`, `disabled`, `identity`; `server` → узел (исходник `uri`/`wg_ini` — текст как есть, `json` и тело без исходника — JSON с тегом записи), `detour` узла; `folder` → папка и члены; `chain` → `SourceChain` из `body` + хопы ссылками; корневые `auto`/`unsupported`/незнакомый вид → `backup_source_kind_unsupported` |
| Члены папки 1.0 | — | `server` — узел; `unsupported` с `origin.raw` — нечитаемый член папки (§234); `chain`/`auto` — `backup_source_kind_unsupported`: цепочка у LxBox только корневой источник, провайдерской группы в модели нет |
| Правила 1.0 | — | кодек `ruleFromRecord`; `inline` с ключом тела, которого модель не держит (`method: drop`, `action` кроме `reject`, незнакомый матчер), — вид `json` телом целиком, без потерь; `rule_set` в теле и `srs` с незнакомым ключом — отброс с `backup_unknown_field`; вид `json` в файле 1.0 — `backup_unknown_field` (в 1.0 снят); цель из `body.outbound` проверяется по известным; `preset` без имени зовётся ссылкой |
| DNS 1.0 | — | кодек записей; серверы `user`/`template`/`preset`, правила `user`/`preset`; preset-сервер — `ref` = `<preset_id>:<tag>` (делится по первому `:`, в storage — тег); пресет, которого нет в шаблоне, — `backup_dns_entry_skipped` (и сервер, и правило); `default_domain_resolver` → var `dns_default_domain_resolver` |
| Секции узла | 0.12 `servers[].sections` молча игнорировались | 1.0: отбраковка B3, у каждой отброшенной записи `backup_section_record_dropped` с `kind` и `reason` (`kind` \| `rule_set` \| `unknown_key` — незнакомый LxBox ключ тела); `sections` у подписки/папки/цепочки/`unsupported` — тот же код с `reason: not_allowed`. 0.12-блок по-прежнему игнорируется молча |
| Префикс тегов | `tag.prefix` 0.12 — как есть | у контракта финальный тег = `prefix` + тег (разделитель — часть префикса, `"[P] "`), у LxBox = префикс + пробел + тег: чтение 1.0 срезает хвостовые пробелы, запись дописывает пробел. Без этого `"[P] "` давал у LxBox `"[P]  NL-1"`, а `"PR"` у лаунчера — `"PRnode"` |
| Неизвестные ключи | один обход по таблице 0.12 | второй обход `_scanUnknown10` по таблице 1.0; внутрь `body` и `identity` не спускается; тело цепочки проверяет её разбор |
| Слияние подписок | новый `id` всегда свежий | новый берёт `id` файла, если он свободен и годится в идентификатор; у записи 1.0 отсутствующий интервал = умолчание 24 ч; возвращает карту `id` файла → `id` здесь и место новых в файле |
| Слияние узлов и папок | папка по имени, узел по телу; новые папки и узлы в порядке «сперва папки» | папка 1.0 — сперва по `id`, затем по имени только среди папок, существовавших до импорта; совпавшая держит свой `id` и имя, берёт `enabled` и префикс файла; папка 0.x — по имени, настройки не трогает. Совпавший по телу узел: поле `sections` есть — замещает целиком (пустое снимает), нет — свои остаются. Новый узел получает личный detour по ссылке. Новые источники всех видов встают в конец в порядке файла (§9 п. 8) |
| Позиции цепочек и detour | — | `folder_id` через карту контейнеров (папки и подписки) → префикс контейнера + сырой тег; контейнера нет — сырой тег как есть |
| Ось правил | разбор сортировал по `num` (без `num` — вперёд), номера файла сохранялись | корневые и узловые на одной оси; номера файла сохраняются (см. «Ось правил»), неразмеченные корневые встают в хвост оси |
| DNS-слияние | серверы по `kind`+`tag` (preset без тега пропускался), правила по имени | серверы по `kind`+`tag`, preset по `kind`+полный `ref`; второй preset-сервер под тем же тегом storage не держит — пропуск; правила по `kind`+`ref`+телу; дописанное правило без имени получает имя из тела с `-2` при повторе |
| Кодек записей | `action` кроме `reject` читался правилом на `direct-out`; preset-сервер писался и читался `tag` | `action` кроме `reject` — в `unknownKeys`; preset-сервер: `dnsServerToRecord(s, presetId:)` пишет `ref` = `<preset_id>:<tag>`, чтение делит `ref` по первому `:` |
| Канон тела узла | текст резался по первому `#` | многострочный текст (WG-INI) не режется: `#` там комментарий |

### Запись

| Слой | Было (0.12) | Стало (1.0) |
|---|---|---|
| Версия | `lx_backup: 1` | одна константа `kLxBackupVersion = 2`: пишем 2, читаем 2 и legacy 1 |
| Источники | `subscriptions[]` + `servers[]` (члены папки — записи с `folder`) + `chains[]` | один `sources[]`: подписки, одиночные узлы и папки в порядке списка, следом цепочки (так их показывает экран источников) |
| Подписка | `label`, `tag{prefix}`, `update`, `disabled`, `identity` | `kind`, `enabled`, `id`, `name`, `tag_policy{prefix}` (с разделителем), `url`, `identity`, `update{interval_hours}` всегда, `disabled` |
| Одиночный узел | `uri` \| `config_json`, `node_tag` | `tag` (имя, у безымянного — тег узла), `enabled`, `origin{kind: uri\|wg_ini\|json, raw}`, `body` у JSON-исходника с одним outbound, `detour{folder_id?, tag}` из `overrideDetour`, `sections`, `id` |
| Папка | члены записями `servers[]`; настройки папки, секции и нечитаемые члены теряются с `backup_local_only_dropped` | `kind: folder`, `enabled`, `id`, `name`, `tag_policy`, `nodes[]`: член `server` с `origin`/`body`/`detour`/`sections`, нечитаемый — `unsupported` с исходником и `reason` |
| Цепочка | `chains[]{tag, label, enabled, chain{…}}` | `kind: chain`, `tag`, `enabled`, `body{type: chain, idle_timeout, strip_evasion, strip, rewrite}`, `hops[]` ссылками |
| Ссылки | позиции цепочки строками | `_LinkIndex`: тег корневого узла → `{tag}`; тег члена папки (префикс + сырой) → `{folder_id, tag}`; тег с префиксом подписки → `{folder_id, tag}`; прочее (Направления, цепочки, служебные) → `{tag}` — обратное импорту |
| Правила | `match` + `outbound`, `ref` + `refs` при 2+; вид `json` терялся | кодек `ruleToRecord` (`body` с целью, `refs[]` всегда, `num`, `dns`/`resolve`); вид `json` — `inline` с телом как есть, массив тел — по записи на тело в том же месте оси |
| DNS | `servers[]{kind, name, value}`, `rules[]{kind, name, ref, value}`, `final`, `strategy` | кодек записей: сервер `user` телом без `tag`, `template` тегом, `preset` — `ref` = `<preset_id>:<tag>` (пресет находится по шаблону); правило `user` телом, `preset` ссылкой; `strategy`, `final`, `default_domain_resolver` |
| Тонкий слой | `directions[]`, `vars`, `route`, `warp[]` | без изменений формы |
| Экран экспорта | — | загружает шаблон ради `preset_id` preset-серверов DNS; версии формата экран не показывает |

## Что LxBox в 1.0 не выражает и как решено

| Данные LxBox | Решение |
|---|---|
| `import_rules`, `import_rules_enabled`, `on_update_action` подписки | дома в 1.0 нет — `backup_local_only_dropped` (реестр: `side: both`, у LxBox — import-rules и настройки папки), одним предупреждением на подписку |
| `detour_policy` подписки и папки (включая `overrideDetour` источника) | `detour` источника — поле лаунчера, флаги политики шире ссылки — `backup_local_only_dropped` |
| флаги `detour_policy` одиночного узла кроме `overrideDetour`; `tag_prefix` одиночного узла | `overrideDetour` едет `detour` узла; прочее — `backup_local_only_dropped` |
| `ping_url`, `ping_timeout_ms` папки | `backup_local_only_dropped` |
| имя цепочки (`label`) | у записи `chain` 1.0 поля нет — `backup_local_only_dropped` |
| правило вида `json` (§225) | `inline` с телом как есть; массив тел — несколько записей; текст, не разбирающийся в тело, — `backup_local_only_dropped` |
| `srs`-правила DNS; `varValues` и `description` DNS-сервера | `backup_local_only_dropped` одной строкой на секцию |
| DNS-правила вида `template` | не пишутся: это ссылки на правила шаблона, которые сторона заводит сама по своему шаблону, пользовательской настройки в них нет |
| `origin` одиночного узла (`paste`/`manual`, write-only диагностика §219) | не пишется; импорт ставит `manual` |
| исходник члена папки, который не разобрался | `kind: unsupported` с `origin.raw` (импорт возвращает его нечитаемым членом) |

BACKUP.md говорит, что писатель 1.0 `backup_local_only_dropped` не эмитит: у
лаунчера полей без дома не осталось. У LxBox они есть, и реестр после синка
1.0.0 это прямо называет (`side: both`, «у LxBox — import_rules, свои
настройки папки, политика detour источника»), поэтому код остался в писателе.

## Ось правил

Норма (§9 п. 7) — относительный порядок сохраняется, абсолютные номера у
сторон свои. Лаунчер перенумеровывает подряд от 1000. Шаг 1 (86803db6) сделал
так же, и у LxBox это ухудшало то, что идёт ПОСЛЕ импорта. Раскладка шаблона
LxBox: `traffic-processing` 0, `private-ip` 950, `block-ads` 960, `fcm-push`
970, `bittorrent` 980, `vowifi` 990, пользовательская зона 1000–1100,
`ru-inside` 1110, `ru-direct` 1120, `fakeip` 1130, `unknown-traffic` 1150.

Пример — файл LxBox `[tp 0, private-ip 950, block-ads 960, A 1000, B 1001, ru-inside 1110, ru-direct 1120]`:

| | v2.23.2 | шаг 1 (1000+позиция) | сейчас |
|---|---|---|---|
| номера после импорта | 0, 950, 960, 1000, 1001, 1110, 1120 | 1000–1006 | 0, 950, 960, 1000, 1001, 1110, 1120 |
| новое правило из UI (`nextUserRuleNum`) | 1002 — перед `ru-inside` | 1007 — после `ru-direct` | 1002 — перед `ru-inside` |
| включённый позже `fcm-push` (970) | между 960 и 980 | перед `traffic-processing` (1000): `sniff` не первый | между 960 и 980 |

Решение: номер из файла сохраняется (при равных — корневое раньше узлового, как
у сборки), неразмеченные корневые встают в хвост оси, правило секции без номера
остаётся без него (сборка ставит 945). Досев шаблона на загрузке ставит свой
номер только отсутствующему несортируемому `traffic-processing` (0 — голова), и
среди пользовательских правил он не встаёт. Корпус сверяет порядок, а не
номера, — `order_renumbered_preserving_sequence` и `v10_node_sections` зелёные.

## Контракт и корпус

- Синк `contract/` с лаунчером (c75b74d3, `contract.lock` sha `fa4250d0`):
  VERSION 1.0.0, D-110, `backup_source_kind_unsupported` — `side: both`, у
  `backup_section_record_dropped` reason `unknown_key`, в корпусе
  `v10_sources_union.expected.lxbox.json` (член папки `chain`, `postfix`,
  `replace_tags` — граница модели LxBox). Временное ожидание стороны из шага 1
  (`app/test/contract/backup_pending_overrides/`) удалено, раннер читает
  корпусный override.
- `backup_section_record_dropped` и `backup_source_kind_unsupported` — коды
  LxBox (`_codesInCode`), из `_launcherOnlyBackupCodes` сняты.

## Что не применяется на импорте

По норме BACKUP.md §1 объявленное поле чужой стороны игнорируется молча:
`tag_policy.postfix`, `fold`/`fold_tag`, `detour` источника,
`relays_in_directions`, `skip`, `max_nodes`, `update.auto_refresh`; у узла —
`group`, `service`, `reason` (кроме члена `unsupported`). `folder_id` ссылки у
LxBox не хранится: позиция цепочки и detour — теги конфига, префикс
подставляется в момент импорта; ссылка без контейнера ввозится сырым тегом, и
папка, приехавшая следующим файлом, её уже не подхватит. Исходник `uri` едет как
есть: если имя в ссылке разошлось с `tag`, у члена папки имя будет из ссылки.
Прежняя граница, импортом 1.0 не затронутая: тег нового корневого узла не
уникализируется суффиксом `-2` (§9 п. 2).

## Тесты

- корпус `backup/*` (23 кейса) своим раннером: все 0.12-кейсы по прежним
  ожиданиям; `v10_dns_body`, `v10_node_sections`, `v10_rules_body_action` — по
  базе; `v10_sources_union` — по корпусному override LxBox. Раннер сверяет
  `warning_reasons`, ось с узловыми правилами, `outbound` (тег/`reject`/`drop`)
  и `vars` правила, `folder_ids`, `chains[].hops` (позиция → узел состояния),
  `dns` (серверы с телом, число правил, три скаляра), `sections`. Сверки
  проверены порчей кода;
- `test/contract/lx_backup_roundtrip_test.dart`: экспорт — форма 1.0, порядок
  ключей корня, валиден по `schema/backup.schema.json` (валидатор
  `json_schema_lite.dart` в объёме схемы; негативный случай и файлы `v10_*`
  корпуса); ссылки (префикс папки и подписки → `folder_id`, detour, `unsupported`,
  json → inline, `ru-direct:yandex_udp`); круг «состояние → экспорт → импорт в
  пустое» = исходное состояние (подписка, узлы URI и JSON, две папки-тёзки,
  секции, detour, нечитаемый член, цепочки, правила всех видов, DNS всех видов,
  Направление с бюджетом, `vars`, `route.final`) и тот же файл при повторном
  экспорте; импорт собственного экспорта в совпадающее состояние ничего не
  добавляет; потери без дома названы;
- `test/contract/lx_backup_v10_test.dart`: версии и отказы, тёзки папок,
  настройки совпавшей папки, хопы, секции совпавшего узла, виды записей без
  дома, ось правил (номера файла, равные, хвост), правила и DNS 1.0, кодек,
  канон WG-INI, причины B3 (включая `unknown_key`); импорт настоящего файла 1.0
  лаунчера `test/fixtures/lx_backup/launcher_v8_export10.json` (Export10 над
  `core/state/testdata/v8_roundtrip.json`): ровно три ожидаемых предупреждения
  (член папки `auto`, preset-сервер и preset-правило DNS чужого пресета
  `russian`), порядок источников, detour и хоп в подписку с префиксом `[P] `;
- прежние тесты экспорта 0.12 (`lx_backup_test`, `lx_backup_sections_test`)
  переведены на форму 1.0.

Не проверялось на устройстве: экспорт и импорт из диалога экрана бэкапа.

## Вопросы лаунчеру

Закрыты ответами 14.09.2026: override корпуса, `side: both`, `unknown_key`,
форма `ref` preset-сервера. Открыты:

1. `detour` в BACKUP.md §2 помечен «launcher»; LxBox пишет и читает `detour`
   узла (личный detour сервера и члена папки) — отметить поддержку «обе»?
2. Имени цепочки (`label`) в записи `chain` 1.0 нет, в 0.12 оно было полем
   LxBox (D-094) — завести?
3. Номера оси: LxBox сохраняет номера файла (таблица выше), лаунчер
   перенумеровывает от 1000 — зафиксировать в норме, что сторона вправе
   сохранить номера при сохранённом порядке.
