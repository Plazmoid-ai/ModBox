# 443 — Контракт 1.0.2 и доводка до нормы SPEC 129

| Поле | Значение |
|------|----------|
| Статус | **Released в v2.24.0** (15.09.2026, ядро `v1.14.0-lx.39`). Done. Device-verify не проводился: изменения — сборка конфига, импорт и валидатор шаблона; форма конфига проверена `sing-box check` |
| Дата | 2026-09-15 |
| Коммиты | `chore(contract)` d3675240 — синк 1.0.2; `test(443)` 3f76b3f0 — L7 и раннер корпуса; `fix(443)` 47bff15a, 11c400a7 — вторая линия; `docs(443)` ef25d900 — SPEC 128 → 129 в ссылках §441; `fix(443)` 0bd7c98b — Н11; `fix(443)` 60d206d1 — порядок импорта; `docs(443)` — эта таска, GUARDS, CHANGELOG |
| Норма | SPEC 129 лаунчера — [`129-F-N-DNS_TEMPLATE_VARS/SPEC.md`](../../../../singbox-launcher/SPECS/129-F-N-DNS_TEMPLATE_VARS/SPEC.md) (Н10, Н11, §5.5, §9); решение D-118 (`SPECS/103-F-O-LX_SHARED_CONTRACT/DECISIONS.md`, заменяет D-117 п. 2); контракт 1.0.2, `contract/TASKS_LXBOX.md` `## 18`; README корпуса `contract/corpus/backup/README.md`, раздел «Фикстура объявлений шаблона». Лаунчер develop `8971d62c` |
| Связанные | [§441](441-template-preset-vars-in-record.md) (значения переменных в записи — основная часть L1–L9), §419 (лечение резолверов), §312 (DNS-группы), §435 (Tailscale, NODE_SECTIONS §6) |

## Что было после §441

§441 делал L1–L9 по черновику спеки, до выкладки контракта. Остались: синк
1.0.2 и корпус, L7, форма второй линии fail-closed, которую лаунчер
доопределил после черновика (у §441 `dns.final` на выпавший сервер
заменялся политикой §419), Н11 и порядок импорта §5.5.

## Что сделано

| # | Норма | Где | Что |
|---|---|---|---|
| 1 | контракт 1.0.2 | `app/contract.lock` | Синк с лаунчера develop `8971d62c` |
| 2 | L7 | `test/contract/lx_backup_test.dart` | `_pendingPortableVars` снят: пять имён D-117 в реестре `portable: false`, `kLxPortableVars` сходится с реестром без исключений |
| 3 | §9.1, README корпуса | `test/contract/backup_corpus_test.dart` | Фикстура `<case>.template.json` — в оба импорта; отсутствие `vars` у записи DNS в ожидании = «ожидаем пусто» у любого кейса (как Go-раннер); `warning_reasons` — множеством. Предсостояние импортируется без целей сцены `{proxy, direct}`, как у Go-раннера (`ImportOptions{RecordVars}`): с целями `google_dot` предсостояния выключался Н9, хотя `vpn-1` привозит сам кейс. 31 кейс зелёный |
| 4 | Н10 | `post_steps/heal_detour_dropped_dns.dart`, `post_steps/dns_servers.dart` | Форма второй линии лаунчера, таблица ниже |
| 5 | Н11 | `template_loader.dart` (`validateTemplateConstructs`) | `@name` в теле шаблонного DNS-сервера обязан быть объявлен в `vars[]` этого сервера, иначе `TemplateIfError` на загрузке; `#if`/`#enable` тела — с областью видимости переменных сервера. Боевой `wizard_template.json` проходит |
| 6 | §5.5 | `lx_backup_import.dart` (`planLxBackupImport`) | План сперва приводит `vars` template-серверов приёмника к нормам (Н2/Н3/Н4, молча), потом накладывает файл. `rules[]` приёмника в план не входят: секция замещается правилами файла, их `vars` нормализуются там же |

### Вторая линия fail-closed (Н10)

| Ссылка на сервер, выпавший из-за висячего detour | §441 | §443 (норма) |
|---|---|---|
| `dns.rules[].server` | `action: reject`, ключи маршрута сняты | без изменений |
| `dns.final` | замена умолчанием шаблона `dns_final` или первым пригодным | ключ снят, последним DNS-правилом `{"action": "reject"}` без условий |
| `route.default_domain_resolver` | замена; нечем — ссылка оставалась (fatal валидатора) | замена умолчанием `dns_default_domain_resolver`, иначе первый эмитированный не `fakeip`/`hosts`; нечем — ключ снят |
| `domain_resolver` узлов (`outbounds[]`, `endpoints[]`) | не лечился | как у `route` |
| `domain_resolver` DNS-сервера | замена кроме себя; нечем — ключ снят | адрес сервера — IP или адреса нет: ключ снят; имя — замена кроме себя, нечем — снят |
| значение-объект `{server, strategy, …}` | не распознавалось | заменяется `server`, форма сохраняется |
| DNS-группа, опустевшая от членов второй линии | эмитилась пустой, fatal `EmptyDnsGroup` | выпадает и лечится как сервер (вложенные группы — тоже) |
| сервер, выпавший из-за висячего `endpoint` (Tailscale) | прежний механизм | без изменений |

Замены по-прежнему не персистятся. Конфиг, где вторая линия не сработала,
не меняется ни в одном байте.

## Отклонения

- **Сам себе не резолвер.** У лаунчера DNS-сервер, для которого замена
  совпала с ним самим, ключ теряет; у LxBox берётся следующий пригодный, и
  только если его нет, ключ снимается. Норма случай не описывает.
- **Порядок импорта у LxBox сохранности `dns_ip` не менял.** Наложение и так
  шло поверх записи приёмника, а нормализация — после; потеря у лаунчера
  (корневые `dns_<tag>_<var>` в несохранённом состоянии) у LxBox не
  воспроизводится — склеенных имён в хранении нет. Наблюдаемое изменение —
  записи приёмника, которых файл не коснулся, в плане (превью, раннер
  корпуса) приходят нормализованными.
- **Раннера DNS-корпуса у LxBox нет** (`contract/corpus/dns/`): кейс
  `nested_vars_substituted` с `state[].vars` не гоняется.

## Проверка

- Тесты: `test/builder/dns_detour_fail_closed_test.dart` (заглушка `final`,
  резолверы узлов, объект-резолвер, IP-адрес, «нечем заменить», неизменность
  при пустом наборе; группы и Tailscale через `resolveDnsServersBodies`;
  `rich_v0` и `avd_v0` из хранения), `test/builder/enable_load_validation_test.dart`
  (Н11), `test/services/dns/dns_template_vars_import_test.dart` (§5.5),
  `test/contract/backup_corpus_test.dart` (31 кейс),
  `test/contract/lx_backup_test.dart` (L7).
- `sing-box check` бинарём `sing-box-lx` `v1.14.0-lx.39-1-g3916231d3`: конфиг
  `avd_v0` с выпавшим `google_udp` (правило → reject, без `final`, заглушка
  последним правилом, резолверы серверов заменены `dns_shield`) — exit 0; тот
  же конфиг с объектными `route.default_domain_resolver` и `domain_resolver`
  узла — exit 0.
